<?xml version="1.0"?>
<definitions name="cleanUpProcess"
        targetNamespace="http://bpel4mobile.com/example/hotel/schemas"
        xmlns:tns="http://bpel4mobile.com/example/hotel/schemas"
        xmlns:plnk="http://docs.oasis-open.org/wsbpel/2.0/plnktype"
        xmlns="http://schemas.xmlsoap.org/wsdl/"
        xmlns:soap="http://schemas.xmlsoap.org/wsdl/soap/"
        >

<!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
     TYPE DEFINITION - List of services participating in this BPEL process 
     The default output of the BPEL designer uses strings as input and 
     output to the BPEL Process. But you can define or import any XML 
     Schema type and us them as part of the message types.
     ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->    
    <types>  
        <schema attributeFormDefault="unqualified" 
                elementFormDefault="qualified" 
                targetNamespace="http://bpel4mobile.com/example/hotel/schemas" 
                xmlns="http://www.w3.org/2001/XMLSchema"
                >
                
            <element name="cleanUpRequest">
                <complexType>
                    <sequence>
                    	<element name="deadline" type="dateTime" />
                    	<element name="room" type="tns:roomComplexType"></element>
                    </sequence>
                </complexType>
            </element>
                  
            <element name="cleanUpResponse">
                <complexType>
                    <sequence>
                    	<element name="status" type="string" />
                    	<element name="room" type="tns:room"></element>
                    </sequence>
                </complexType>
            </element>
            
        
            <complexType name="room">
            	<sequence>
            		<element name="id" type="int"></element>
            	</sequence>
            </complexType>
        
        
            <complexType name="roomComplexType">
            	<sequence>
            		<element name="id" type="int"></element>
            		<element name="number" type="int"></element>
            		<element name="floor" type="int"></element>
            		<element name="category" type="tns:roomCategory"></element>
            	</sequence>
            </complexType>
        
            <complexType name="roomCategory">
            	<sequence>
            		<element name="name" type="string"></element>
            		<element name="standard" type="int"></element>
            	</sequence>
            </complexType>
        </schema>
    </types>
  
<!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
     MESSAGE TYPE DEFINITION - Definition of the message types used as 
     part of the port type defintions
     ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->    
    <message name="cleanUpProcessRequestMessage">
        <part name="payload" element="tns:cleanUpRequest"/>
    </message>
    
    <message name="cleanUpProcessResponseMessage">
        <part name="payload" element="tns:cleanUpResponse"/>
    </message>


<!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
     PORT TYPE DEFINITION - A port type groups a set of operations into
     a logical service unit.
     ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->
    <!-- portType implemented by the cleanUpProcess BPEL process -->
    <portType name="cleanUpProcess">
        <operation name="initiate">
            <input message="tns:cleanUpProcessRequestMessage"/>
        </operation>
    </portType>

    <!-- portType implemented by the requester of cleanUpProcess BPEL process 
         for asynchronous callback purposes
         -->
    <portType name="cleanUpProcessCallback">
        <operation name="onResult">
            <input message="tns:cleanUpProcessResponseMessage"/>
        </operation>
    </portType>


<!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
     PARTNER LINK TYPE DEFINITION
         the cleanUpProcess partnerLinkType binds the provider and
         requester portType into an asynchronous conversation.
     ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->    
    <plnk:partnerLinkType name="cleanUpProcess">
        <plnk:role name="cleanUpProcessProvider" portType="tns:cleanUpProcess"/>
        <plnk:role name="cleanUpProcessRequester" portType="tns:cleanUpProcessCallback"/>
    </plnk:partnerLinkType>   

<!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
     BINDING DEFINITION - Defines the message format and protocol details
     for a web service.
     ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->
    <binding name="cleanUpProcessBinding" type="tns:cleanUpProcess">
        <soap:binding style="document"
    		transport="http://schemas.xmlsoap.org/soap/http"/>
        <operation name="initiate">
            <soap:operation
    			soapAction="http://bpel4mobile.com/example/hotel/schemas/initiate"/>
            <input>
                <soap:body use="literal"/>
            </input>
        </operation>
    </binding>

    <binding name="cleanUpProcessCallbackBinding" type="tns:cleanUpProcessCallback">
        <soap:binding style="document" transport="http://schemas.xmlsoap.org/soap/http"/>
        <operation name="onResult">
            <soap:operation
    			soapAction="http://bpel4mobile.com/example/hotel/schemas/onResult"/>
            <input>
                <soap:body use="literal"/>
            </input>
        </operation>
    </binding>

<!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
     SERVICE DEFINITION - A service groups a set of ports into
     a service unit.
     ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->
    <service name="cleanUpProcessService">
        <port name="cleanUpProcessPort" binding="tns:cleanUpProcessBinding">
    		<soap:address location="http://localhost:8080/ode/processes/cleanUpProcess"/>
        </port>
    </service>

    <service name="cleanUpProcessServiceCallback">
        <port name="cleanUpProcessPortCallbackPort" binding="tns:cleanUpProcessCallbackBinding">
    		<soap:address location="http://localhost:8181/hotel/ws/cleanUpServiceCallback"/>
        </port>
    </service>
</definitions>